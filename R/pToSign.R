



# pToSign -----------------------------------------------------------------

pToSign = function(x){

  ELSE=TRUE
  case_when(
    (x>0.05) ~ 'ns',
    (x<0.0001) ~ '****',
    (x<0.001) ~ '***',
    (x<0.01)~ '**',
    ELSE ~ '*'
  )

}

